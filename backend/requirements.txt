# Core Dependencies (Required)
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
opencv-python>=4.8.0
pillow>=10.0.0
pdf2image>=1.16.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
requests>=2.31.0
python-multipart>=0.0.6
gunicorn>=21.2.0
transformers>=4.33.0
nltk>=3.8

# OCR Engines (Optional - one of these required for word-level OCR)
easyocr>=1.7.0
pytesseract>=0.3.10

# PyTorch (Optional - uncomment for GPU acceleration)
# torch>=2.0.0
# torchvision>=0.15.0
# LayoutParser + Detectron2 (Recommended for real CNN confidences)
# LayoutParser requires Detectron2 and PyTorch. Installing Detectron2 must match
# your CUDA/PyTorch build — see https://detectron2.readthedocs.io/en/latest/tutorials/install.html
# Recommended (CPU-only) install steps:
# 1. Install CPU PyTorch wheel (example):
#    pip install "torch>=2.0.0+cpu" torchvision --index-url https://download.pytorch.org/whl/cpu
# 2. Install Detectron2 (CPU wheel) matching your torch version — check detectron2 docs for the correct -f URL
# 3. Install LayoutParser: pip install layoutparser
# Alternatively, use conda for simpler dependency handling.

# Advanced Layout Analysis
layoutparser>=0.3.4

# Google Translation (Optional - uncomment for backend translation)
# google-cloud-translate>=3.12.0

